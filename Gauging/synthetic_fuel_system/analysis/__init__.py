"""
Geometry-agnostic fuel gauging analysis toolkit.

This package provides import, interpolation, comparison, and visualization
tools for real and synthetic fuel system H-V table data. It is independent
of the synthetic simulation in ``src/`` — only the optional
``synthetic_bridge`` importer references that code.

Subpackages
-----------
importers : Data import from .mat (MM1D), CSV, and synthetic bridge
"""
